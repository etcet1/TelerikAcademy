using System;

/* 4.Declare an integer variable and assign it with the value 254 in hexadecimal format.
 * Use Windows Calculator to find its hexadecimal representation.
 */

class IntegerVariableInHexadecimalFormat
{
    static void Main()
    {
        //The integer 254 in hexadecimal is FE.
        int numberInHex = 0xFE;
        Console.WriteLine(numberInHex);
    }
}

